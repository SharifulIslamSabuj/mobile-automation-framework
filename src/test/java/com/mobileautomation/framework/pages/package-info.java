/**
 * Page Object classes — one per screen, encapsulating locators and screen
 * interactions only. No business logic and no assertions belong here.
 * Implemented in Phase 9.2: {@code LoginPage}, {@code ProductsPage},
 * {@code ProductDetailsPage} — the minimum set required by the Pilot
 * (TC-004, TC-012), built to enterprise/reusable standard rather than
 * scoped narrowly to just those two flows. See
 * docs/framework/FRAMEWORK_ARCHITECTURE_RULES.md.
 */
package com.mobileautomation.framework.pages;
