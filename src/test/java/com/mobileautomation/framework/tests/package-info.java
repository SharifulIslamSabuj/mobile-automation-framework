/**
 * TestNG test classes containing test logic and assertions. Implemented in
 * Phase 9.4/9.5, completing the two-test Pilot: {@code LoginTest} (TC-004
 * — Login Outcome Verification) and {@code ProductDetailsTest} (TC-012 —
 * Add to Cart Action). Both extend {@code core.BaseTest}; neither contains
 * a locator, direct driver access, or a direct reporting/logging call —
 * all of that is inherited or delegated to {@code pages}/{@code assertions}.
 */
package com.mobileautomation.framework.tests;
