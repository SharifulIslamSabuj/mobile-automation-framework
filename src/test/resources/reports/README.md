# Test Resources — Reports

Reserved for static, version-controlled reporting resources (e.g., ExtentReports theme/config templates). This is **not** the output location for generated report runs — generated reports are written outside `src/` and are excluded via `.gitignore` (`/reports/`, `/test-output/`).

No implementation exists yet — this directory is part of the frozen project skeleton (Project Bootstrap phase).
