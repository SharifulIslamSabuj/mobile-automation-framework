# Test Resources — Screenshots

Reserved for version-controlled reference/baseline screenshot assets bundled with tests. This is **not** the output location for screenshots captured during test runs — captured screenshots are written outside `src/` and are excluded via `.gitignore`.

No implementation exists yet — this directory is part of the frozen project skeleton (Project Bootstrap phase).
