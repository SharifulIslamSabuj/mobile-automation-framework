# Test Resources — Test Data

Reserved for test data files (e.g., JSON/YAML datasets consumed by the Test Data layer, corresponding to the datasets defined in MA-TDD-001).

No implementation exists yet — this directory is part of the frozen project skeleton (Project Bootstrap phase).
