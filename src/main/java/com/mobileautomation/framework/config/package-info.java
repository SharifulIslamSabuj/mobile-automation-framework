/**
 * Configuration loading and environment/platform property resolution — the
 * single source of truth for every configurable value in the framework.
 * <p>
 * See {@link com.mobileautomation.framework.config.ConfigReader} as the
 * entry point, and {@code docs/framework/CONFIGURATION_ARCHITECTURE.md} for
 * the full loading order, hierarchy, and environment-switching mechanism.
 */
package com.mobileautomation.framework.config;
