/**
 * Test data loading infrastructure, implemented in Phase 8 (Test Data
 * Framework) across sub-packages: {@code manager} (the single entry point,
 * {@code TestDataManager}), {@code loader} ({@code DataLoader}),
 * {@code reader} (+ {@code json}/{@code yaml}/{@code properties}),
 * {@code validator}, {@code environment}, {@code provider} (negative-data
 * library), {@code generator} (Faker/random/dynamic), and {@code factory}.
 * The POJO models themselves live in the sibling {@code models} package,
 * per that package's own Project-Bootstrap description. See
 * docs/framework/TEST_DATA_FRAMEWORK.md.
 */
package com.mobileautomation.framework.data;
