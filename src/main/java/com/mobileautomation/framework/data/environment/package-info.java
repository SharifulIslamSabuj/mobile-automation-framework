/**
 * Environment-aware test-data resource resolution —
 * {@code TestDataEnvironmentResolver} resolves a relative resource path
 * against the active {@code config.Environment}'s folder, falling back to a
 * common folder. Configuration-driven, no hardcoded environment name. See
 * docs/framework/TEST_DATA_FRAMEWORK.md.
 */
package com.mobileautomation.framework.data.environment;
