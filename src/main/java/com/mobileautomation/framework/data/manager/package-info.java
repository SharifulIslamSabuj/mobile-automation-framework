/**
 * The single entry point every future test uses for test data —
 * {@code TestDataManager}. Hides {@code data.reader}/{@code data.loader}
 * and exposes environment-aware loading plus factory access. See
 * docs/framework/TEST_DATA_FRAMEWORK.md.
 */
package com.mobileautomation.framework.data.manager;
