/**
 * Reads, deserializes, validates, and caches test-data resources —
 * {@code DataLoader}. Selects a {@code data.reader} implementation by file
 * extension so consumers never choose a reader themselves. See
 * docs/framework/TEST_DATA_FRAMEWORK.md.
 */
package com.mobileautomation.framework.data.loader;
