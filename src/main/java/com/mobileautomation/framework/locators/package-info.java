/**
 * Centralized locator repository access.
 * <p>
 * The single source of truth for every UI element locator is
 * docs/automation/LOCATOR_REPOSITORY.md (MA-LOC-001). No implementation exists
 * yet — this package is part of the frozen project skeleton (Project Bootstrap
 * phase). Page Objects must never hardcode a locator; they must resolve it
 * through this package once implemented.
 */
package com.mobileautomation.framework.locators;
